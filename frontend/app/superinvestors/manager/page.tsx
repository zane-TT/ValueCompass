import ManagerDetailClient from "./ManagerDetailClient";

export const metadata = {
  title: "Manager Portfolio | ValueCompass",
  description: "DATAROMA manager portfolio holdings, concentration, and quarterly activity summary.",
};

export default function ManagerDetailPage() {
  return <ManagerDetailClient />;
}
